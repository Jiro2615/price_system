# CODEX_PROMPT.md

C:\price_system を見てください。  
まず `AGENTS.md` と `README.md` を読んで、既存方針を守ってください。

## 目的
楽天価格・在庫更新・出品管理システムを完成させたいです。

## 現在の重要方針
- 日常の価格更新はAmazonページチェック後、必要なものをAPIで少量更新する。
- 在庫更新はInventory API bulk-upsertを使う。
- 楽天CSV更新は、価格計算ロジック変更などの全体更新時に使う。
- CSVでエラーになった商品はblocked化し、次回CSVから除外してAPI更新へ回す。
- 出品管理は後回しだが、最終的には現在の有料出品ツールを置き換えたい。
- メール処理は後で入れる。Amazon発送メール、配達ボックス通知メール、楽天CSV結果メールを解析したい。

## 今やってほしいこと
1. 既存スクリプトの構造を確認する。
2. 認証情報の直書きがあれば `.env` 読み込みに寄せる案を出す。
3. blocked商品をAPI更新へ回すスクリプト設計をする。
4. 既存のdry-run/execute安全設計を壊さない。
5. 変更前に作業方針を説明する。
6. `--execute` 系は勝手に実行しない。

## 禁止
- 本番価格や在庫を勝手に更新しない。
- SFTPアップロードを勝手に実行しない。
- APIキーやパスワードをコードに書かない。
- CSVの文字コードをUTF-8へ戻さない。
